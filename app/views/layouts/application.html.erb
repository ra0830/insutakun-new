<!DOCTYPE html>
<html>
    <div class="container-fluid">
      <head>
        <title>Instakun</title>
        <p>写真にコメントを添えて投稿しよう！</p>
          <%= csrf_meta_tags %>
          <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
          <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
      </head>
      <body>
        <nav class="menu">
          <ul class="row">
            <% flash.each do |key, value| %>
            <%= content_tag(:div, value, class: "#{key}") %>
            <% end %>
        <nav class="menu">
          <ul class="row">
            <% if logged_in? %>

              <li class="col-sm-6"><%= link_to "Profile", user_path(current_user.id), class: 'btn btn-success btn-sm' %></li>

              <li class="col-sm-6"><%= link_to "投稿一覧", feeds_path, class: 'btn btn-success btn-sm' %></li>

              <li class="col-sm-6"><%= link_to "お気に入り一覧", favorites_path, class: 'btn btn-success btn-sm' %></li>

              <li class="col-sm-6"><%= link_to "Logout", session_path(current_user.id), method: :delete, class: 'btn btn-success btn-sm' %></li>

            <% else %>
              <li class="col-sm-6"><%= link_to "Sign up", new_user_path, class: 'btn btn-success btn-sm' %></li>
              <li class="col-sm-6"><%= link_to "Login", new_session_path, class: 'btn btn-success btn-sm' %></li>

            <% end %>
            <%= yield %>
          </ul>
        </nav>
      </body>
    </div>
</html>
